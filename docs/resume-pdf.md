# Résumé placeholder

Place the final public résumé at
`public/resume/sergii-khomenko-software-engineer-cv.pdf`, overwriting the file
already there. The path is set by `resumePath` in `src/data/site.ts`; if you
publish under a different filename, change it there too or the site will keep
serving the old PDF.

Do not use the older private résumé without first removing the home address,
phone number, and any confidential information.
